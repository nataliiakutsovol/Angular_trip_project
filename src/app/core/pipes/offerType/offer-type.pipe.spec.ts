import { OfferTypePipe } from './offer-type.pipe';

describe('OfferTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferTypePipe();
    expect(pipe).toBeTruthy();
  });
});
